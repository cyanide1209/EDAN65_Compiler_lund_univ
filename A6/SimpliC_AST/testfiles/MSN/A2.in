int main() {
    // depth = 1
    int i = 100;
    while (i > 0) {
        // depth = 2
        i = i - 1;
    }
    if (i == 0) {
        // depth = 2
        print(i);
    }
    // depth = 1
return 0;
}